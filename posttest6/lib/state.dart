import 'package:flutter/material.dart';
import 'package:get/get.dart';

class jumlah extends GetxController {
  var total = 0.obs;
  kurang() => total--;
  tambah() => total++;
}

class jumlah1 extends GetxController {
  var total = 0.obs;
  kurang() => total--;
  tambah() => total++;
}

class jumlah2 extends GetxController {
  var total = 0.obs;
  kurang() => total--;
  tambah() => total++;
}

class jumlah3 extends GetxController {
  var total = 0.obs;
  kurang() => total--;
  tambah() => total++;
}

class jumlah4 extends GetxController {
  var total = 0.obs;
  kurang() => total--;
  tambah() => total++;
}

class jumlah5 extends GetxController {
  var total = 0.obs;
  kurang() => total--;
  tambah() => total++;
}

class jumlah6 extends GetxController {
  var total = 0.obs;
  kurang() => total--;
  tambah() => total++;
}
